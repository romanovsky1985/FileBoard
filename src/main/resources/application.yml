server:
  port: 3113

spring:
  application:
    name: FileBoard
  datasource:
    url: "jdbc:h2:mem:FileBoard;DB_CLOSE_DELAY=-1"
    username: admin
    password: qwerty
  jpa:
    show-sql: true

h2:
  console: enabled